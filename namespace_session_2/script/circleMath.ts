namespace MyMath {
    // export namespace circle {
        const PI = 3.14;
        
        export function calculateCircumference(diameter: number): number {
            return PI * diameter;
        }
    // }
}
